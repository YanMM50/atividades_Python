
import os
os.system("cls || clear")

# função para somar

numeros = []

numeros.append(10)
numeros.append(5)
numeros.append(2)

soma = sum(numeros)
print(f"Soma: {soma}")

# Função para potência.
altura = 1.70
calculo = altura * altura
# calculo = altura ** 2
# calculo = pow(altura, 2) pow calcula a potência